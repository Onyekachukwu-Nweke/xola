# Local environment overrides — copy to .env and fill in real values.
# Never commit .env to source control.

POSTGRES_USER=xola
POSTGRES_PASSWORD=xola
POSTGRES_DB=xola
POSTGRES_PORT=5432

# Connection URL used by sqlx (cargo sqlx migrate run, runtime)
DATABASE_URL=postgres://xola:xola@localhost:5432/xola
